services:
  - type: web
    name: smartattend
    runtime: docker
    plan: starter
    region: frankfurt
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: smartattend-db
          property: connectionString

databases:
  - name: smartattend-db
    plan: basic-256mb
    region: frankfurt
    databaseName: smartattend
    user: smartattend
